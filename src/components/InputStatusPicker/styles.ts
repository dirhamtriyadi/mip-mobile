import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
      fieldLabel: {
        fontSize: 16,
        marginBottom: 10,
        fontWeight: 'bold',
      },
});

export default styles;